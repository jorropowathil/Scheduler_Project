var containerEl = document.getElementsByClassName("container")
var todayDateEl = moment();
$("#currentDay").text(today.format("LL HH:mm"));


function
// Create hour time blocks for each hour of the day using for loops
// Create click events 
